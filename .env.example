# Required only for the optional AI analysis endpoint. Never commit a real key.
GEMINI_API_KEY="replace-with-a-secret"

# Optional server binding overrides.
NODE_ENV="development"
HOST="0.0.0.0"
PORT="3000"
